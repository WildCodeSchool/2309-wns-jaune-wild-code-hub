import { background, defineStyleConfig } from "@chakra-ui/react";

const box =  {
    main: {
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        height: "100vh",
    },
    form: {
        width: "500px",
    },
}

export default box;