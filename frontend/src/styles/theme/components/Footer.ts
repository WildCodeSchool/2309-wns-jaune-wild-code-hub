import { defineStyleConfig } from "@chakra-ui/react";

const Footer = defineStyleConfig({
  baseStyle: {
    fontSize: "1.2rem",
  },

  variants: {
    container: {},
  },
});

export default Footer;
